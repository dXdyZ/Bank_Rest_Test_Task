<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <springProperty name="LOG_DIR" source="logging.file.path" defaultValue="./logs"/>

    <property name="MARKER_APP_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss} %-5level | %logger{36} | ip=%X{ip} | %msg%n"/>

    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <!-- Файловый аппендер для LOGIN -->
        <appender name="LOGIN" class="ch.qos.logback.core.FileAppender">
            <file>${LOG_DIR}/LOGIN.log</file>
            <encoder>
                <pattern>${MARKER_APP_PATTERN}</pattern>
            </encoder>
            <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
                <evaluator class="ch.qos.logback.classic.boolex.OnMarkerEvaluator">
                    <marker>LOGIN</marker>
                </evaluator>
                <onMatch>ACCEPT</onMatch>
                <onMismatch>DENY</onMismatch>
            </filter>
        </appender>

        <!-- Файловый аппендер для AUDIT -->
        <appender name="AUDIT" class="ch.qos.logback.core.FileAppender">
            <file>${LOG_DIR}/AUDIT.log</file>
            <encoder>
                <pattern>${MARKER_APP_PATTERN}</pattern>
            </encoder>
            <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
                <evaluator class="ch.qos.logback.classic.boolex.OnMarkerEvaluator">
                    <marker>AUDIT</marker>
                </evaluator>
                <onMatch>ACCEPT</onMatch>
                <onMismatch>DENY</onMismatch>
            </filter>
        </appender>

        <!-- Аппендер для общих ловогов в файл -->
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_DIR}/SERVICE.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_DIR}/SERVICE-%d{yyyy-MM-dd}.log</fileNamePattern>
                <maxHistory>7</maxHistory>
            </rollingPolicy>
            <encoder>
                <pattern>${FILE_LOG_PATTERN}</pattern>
            </encoder>
            <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
                <evaluator class="ch.qos.logback.classic.boolex.OnMarkerEvaluator">
                    <marker>AUDIT</marker>
                    <marker>LOGIN</marker>
                </evaluator>
            </filter>
        </appender>

        <root level="INFO">
            <appender-ref ref="LOGIN"/>
            <appender-ref ref="AUDIT"/>
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>

        <logger name="com.ulisesbocchio" level="WARN"/>
    </springProfile>
</configuration>